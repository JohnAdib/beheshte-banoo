<?php
namespace cls;
use \lib\saloos;
use \lib\router;

class route
{
	public function _before()
	{
		// var_dump($_SERVER);
		// router::$domain = array('city', 'dev' );

		// if()
		// {
		// 	router::shift_url();
		// 	router::$sub_domain = 'city';
			
		// }
		// var_dump(1);
	}
}
?>
